<%= form_for [:admin, @quest_item] do |f| %>
  <% if @quest_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quest_item.errors.count, "error") %> prohibited this quest_item from being saved:</h2>

      <ul>
      <% @quest_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Название %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
    <div class="field">
      <%= f.label "Продолжительность сеанса(мин)" %><br>
      <%= f.text_field :session_length, class: "form-control" %>
    </div>
  <div class="field">
    <%= f.label "Выпуск билетов" %><br>
    <%= f.collection_select(:quest_status_id, QuestStatus.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>