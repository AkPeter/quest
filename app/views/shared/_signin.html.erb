<%= form_tag('session', method: :post) do %>
    <div class="field field-input-text">
      Почта <%= text_field_tag :email %>
    </div>
    <div class="field field-input-text">
      Пароль <%= password_field_tag :password %>
    </div>
    <div class="actions">
      <%= submit_tag 'Войти' %>
    </div>

<% end  %>